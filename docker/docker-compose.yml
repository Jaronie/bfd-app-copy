# npm i docker-compose
# cd docker
# docker-compose up
# go to mysql workbench and log in
services:
 db:
   image: mysql:latest
   restart: always
   container_name: planktonFieldRun
   ports:
     - "3307:3306" # host port : container port
   environment:
     MYSQL_ROOT_PASSWORD: 060702006
     MYSQL_DATABASE: luxuper_db
     MYSQL_USER: root
     MYSQL_PASSWORD: 06072006
   volumes:
     - ./mysql_data:/var/lib/mysql